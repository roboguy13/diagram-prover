// This is a search strategy to find a minimal configuration for a propagator network that uses `NumericRange`s.

import { NumericRange } from "./NumericRange";

// This miminizes the contents of a propagator network based on a set of cells that are to be minimized.
class Minimizer {
  // private _allCells: Cell<NumericRange>[]
  // private _minimizingCells: Cell<NumericRange>[]

  // constructor(allCells: Cell<NumericRange>[], minimizingCells: Cell<NumericRange>[]) {
  //   this._allCells = allCells
  //   this._minimizingCells = minimizingCells
  // }

  public minimize(): void {
  }

  // private makeCheckpoint(): void {
  //   this._lastCheckpoint = this._allCells.map(cell => cell.clone())
  // }
}
